services:
  - type: web
    name: prasad-voice-agent
    runtime: docker
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: conversation-logs-db
          property: connectionString

databases:
  - name: conversation-logs-db
    plan: free
